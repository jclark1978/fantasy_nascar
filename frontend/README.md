Frontend prototype (Vite + React + TypeScript)

To run locally:

```bash
cd frontend
npm install
npm run dev
```

By default the frontend expects the backend API at `http://localhost:8000`. You can change this by creating a `.env` with `VITE_API_BASE` set accordingly.
